# CONTEXT

- 当前正在做什么：阶段 1“项目与环境初始化”已完成并通过验收。
- 上次停在哪：前端 App Shell、后端健康检查、迁移基线、环境模板和 Render 规划均已落盘；自动化测试通过 15、失败 0。
- 关键决定：前端使用 React + Vite + TypeScript；后端使用 FastAPI + PostgreSQL 异步驱动；必需配置缺失时终止启动；Render 当前只声明前端与 API，免费 API 部署前显式执行迁移，Worker/Redis 留到阶段 10。
- 当前下一步：开始阶段 2“Supabase 数据库”，建立业务表、约束、索引和迁移。
- 重要边界：所有改动仅限 `/Users/a1-6/Documents/Paper Grading`；自动删除真实文件前必须再次确认。
