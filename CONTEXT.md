# CONTEXT

- 当前正在做什么：阶段 14 前一候选 `eb2ca93571e4c9fa8649af4b968c32ad11f18ef2` 已完成
  Funnel、双 release、Sites 私有部署和节点 2.5 页面检查；节点 3 尚未开始，生产环境文件和
  LaunchAgent 均不存在。用户实际 UptimeRobot Free 页面虽与帮助页存在出入，但当前账户界面下可
  稳定执行的免费方案只有一个 5 分钟 Keyword monitor；因此本轮移除全部 Heartbeat URL、环境变量
  和外部 ping，只保留对 `/health/ready` 响应体中 `"status":"ready"` 的免费关键字检查，并把
  `smoke-test.md` 从真实 E2E 改为纯只读页面检查。当前本地门禁 `stage14_predeployment_gate=true`，
  阶段 14 聚焦后端测试 `25 passed, 10 deselected`。下一步是提交并推送新候选，待新 SHA 取得 CI
  8/8 后重做受 SHA 影响的 2.3—2.5。生产数据库未迁移，模型调用、业务写入、实际告警和回滚均未执行。
- 最终验收信号：用户于 2026-07-26 明确确认 `docs/STAGE12_ACCEPTANCE.md` 全部步骤执行通过，阶段 12 已完成。
- 阶段 12 最终迁移版本：`20260722_0017`；用户确认真实项目前向迁移和验收均已通过，真实项目不得为验收回退。
- 阶段 12 本地代码：`0017` 原子冻结模型参数、批次和 `export_items`；导出进程只接受专用最小角色的 `EXPORT_DATABASE_URL`。同一快照生成完全相同的文件名、XLSX 字节和 SHA-256，600 秒租约长于 Worker 硬时限。完成竞争失败后，只有当前令牌能先原子转为 failed 才可删除本次新对象，失租旧 Worker 不删除；连续软超时或三次 Worker 丢失会持久化为明确失败，不会永久卡在 running。
- 阶段 12 Excel：四工作表、100 篇逐表映射、严格布尔/结构/Decimal、公式防护、文本格式和组合后 32767 限制均有回归；本地重开、artifact-tool、捆绑 LibreOffice 和用户确认的真实 Excel 客户端验收均已通过。
- 阶段 12 前端：`/exports` 支持草稿/最终、来源说明、轮询、失败和短时下载；查询缓存按教师隔离，同一失败重试复用幂等键，成功后的新操作换新键。登录返回地址保留 `jobId`；本机浏览器已通过中英文、创建排队、错误安全文案、桌面和 `390 × 844`，横向溢出 0、最终 Console 错误 0/警告 0。验收中发现并修复切换语言后排队提示不更新的问题。
- 阶段 12 真实验收：用户确认真实 Supabase、Storage、双教师、草稿/最终、租约恢复、Excel 和桌面/手机浏览器全部通过；具体步骤保留在 `docs/STAGE12_ACCEPTANCE.md`，项目记录不保存凭据或敏感响应。
- 阶段 12 本地门禁：后端通过 414、失败 0，真实 PostgreSQL 15 项默认排除；前端通过 68、失败 0。Ruff、格式、完整 mypy、lint、类型检查、生产构建、离线迁移升级/回退/再升级、验收脚本语法、阶段 12 密钥扫描和差异检查均通过。
- 阶段 13 边界：数据库与 Storage 70% 提醒、85% 阻断阈值和保留时间必须可配置；原始论文、提取文本与模型原始响应默认保留 30 天，分数、反馈与审计元数据长期保留。
- 阶段 13 安全门禁：自动删除和备份清理在真实启用前必须再次取得用户确认；备份目标必须独立于主 Supabase 项目并在接入前确认，任何凭据都不得写入仓库。
- 阶段 13 本地实现：新迁移 `20260726_0018`；数据库批次与每个 Storage 写入均有权威配额门禁。保留服务提供只读 dry-run、短租约、删除前复核和部分成功收口，默认关闭。备份核心提供独立 AES-256-GCM 流式加密、不可覆盖上传和 manifest 校验，不连接真实目标。
- 阶段 13 本地门禁：后端 463 项通过、19 项真实 PostgreSQL 测试默认排除；前端 70 项通过。Ruff、格式、完整 mypy、lint、类型检查、生产构建和 `0018` 离线升级/回退通过。
- 阶段 13 决策：用户确认本阶段不启用备份；不接入目标、不执行备份写入或清理、不进行恢复演练，后续需要时单独接入和验收。
- 阶段 13 默认规则：暂不启用外部通知；三类原始对象从创建时间起算，仅在对应处理完成后允许清理；教师可标记保留并填写理由，管理员可解除；配额实时查询，外部用量提醒暂不启用，查询失败时拒绝写入。
- 阶段 13 迁移修复：真实回放发现 PostgreSQL 不接受 `pg_catalog.` 限定的 `COALESCE`、`GREATEST`、`LEAST`；`0018` 已改为合法特殊表达式，并新增离线与真实回放回归。数据库大小返回空值时也会明确进入 `unavailable`。
- 阶段 13 真实配额验收：按数据库 500,000,000 字节、Storage 1,000,000,000 字节、桶 `paper-grading-test` 执行；额外 `test` 数据库不单独扣减。用户回传 `stage13_quota_probe_passed`、回滚后两个配额开关均为 false，并最终回传真实 PostgreSQL 验收 `4 passed`；70%/85% 边界、失败关闭和并发预留均通过。
- 阶段 13 保留验收：用户于 2026-07-28 回传保留状态机测试 `17 passed`；三类策略均为关闭、保留 30 天，dry-run 前后对象数均为 0、候选数为 0。第五步在“不执行真实删除”的当前范围内通过，自动清理仍保持关闭。
- 阶段 13 备份验收：用户于 2026-07-28 回传本地备份测试 `8 passed`；备份创建和清理均关闭，目标未配置，备份及恢复记录均为空。第六步在“备份保持未启用”的本阶段范围内通过，未执行真实备份或恢复。
- 阶段 13 自动清理决定：用户于 2026-07-28 确认本阶段保持关闭，以第五步已通过的关闭状态、dry-run 和状态机结果作为最终验收标准；不执行真实删除，后续启用时重新授权并单独验收。
- 阶段 13 最终验收：用户于 2026-07-28 回传真实 PostgreSQL `4 passed`；结合本地门禁、迁移、配额探测、第五步保留关闭验收和第六步备份关闭验收，阶段 13 已完成。自动清理和备份后续启用时重新授权并单独验收。
- 阶段 14 本地实现：新增严格串行且不含部署的 GitHub CI、桌面/`390 × 844`
  Playwright 全流程、独立真实环境测试入口、Sites 前端适配、本机 `launchd` 配置，
  以及部署、回滚、冒烟、监控和故障 runbook。
- 阶段 14 安全修复：签名 URL 精确绑定桶和对象；DOCX 以大小写无关方式拒绝宏，并拒绝外部关系、OLE 内嵌对象和 ActiveX；PDF 拒绝脚本、外部动作、附件和交互表单；HTTP 客户端不继承系统代理；配置、SQL 参数和日志隐藏敏感输入；CORS、SSRF、提示注入、公式注入与密钥扫描均有失败门禁。新前向迁移 `20260728_0019` 允许既有评分 Worker 最小角色登录并执行 Storage 配额预留/收口，不增加表权限，生产密码不进入迁移或仓库。
- 阶段 14 当前本地门禁：后端 `491 passed`、失败 0，29 项真实 PostgreSQL/外部测试默认排除；前端 73 项通过、失败 0；本地浏览器 2 项通过、失败 0。Ruff、格式、严格 mypy、lint、类型检查、生产构建、依赖审计、源码/构建密钥扫描和差异检查通过；CI 另配置一次性 PostgreSQL 空库真实升级、回退和再升级。
- 阶段 14 外部边界：用户于 2026-07-30 确认第 6.1 节及之前全部完成；第 6.2 节按
  组合证据完成。Render 付费资源不再属于当前方案；Sites、Mac、Funnel、HTTPS、告警和
  回滚必须在新目标环境验收。自动清理和备份继续关闭，真实恢复按阶段 13 决定移出
  阶段 14 完成条件；目标 Supabase 的 Database Network Restrictions 仍需记录全网放行
  例外。
- 阶段 14 首次 PostgreSQL 验收：用户在保存阶段 12 导出和评分历史的旧独立测试项目运行，结果 4 通过、4 失败；数据库保护函数拒绝回退并保留历史，生产迁移因缺少变量未执行。真实日志同时暴露并修复当前头教师策略误把新增 `export_items` 策略当异常、Worker 最小权限目录误要求评分表为空两项测试假失败。完整回放必须改用全新可销毁项目和独立 `.env.stage14-postgres-test`。
- 阶段 14 第二次 PostgreSQL 验收：全新项目环境文件结构、权限、项目标识和用户 UUID 均正确，但 8 项均因数据库 `InvalidPasswordError` 停止，未开始迁移。数据库密码和 Supabase Secret Key 曾出现在对话中，必须在 Dashboard 轮换并删除旧 Secret Key；轮换后先对 direct 与 Session Pooler 各执行只读连接预检，再重跑 4.1。
- 阶段 14 第三次 PostgreSQL 验收：两个数据库入口均通过只读预检，真实命令得到 6 通过、2 失败。第一项暴露当前头权限测试仍沿用阶段 4 的 `exports INSERT` 和 `teacher_reviews INSERT/UPDATE` 旧授权期望，现已改为 `0019` 精确权限；第二项确认全新项目缺少阶段 13 并发配额测试固定依赖的私有 bucket `paper-grading-test`，4.1 已补充明确前置条件。数据库配置对象 repr 同时改为隐藏两个 URL。普通后端回归 `489 passed, 29 deselected`，4.1 等待用户创建私有测试 bucket 后重跑。
- 阶段 14 第四次 PostgreSQL 验收：私有测试 bucket 建立后得到 7 通过、1 失败；唯一失败是重复执行时测试额外尝试 `0019 → base`，暴露已发布 `0012` 的历史 downgrade 未撤销 Worker 对象权限。阶段 14 必需的首次空库升级以及 `0019 → 0017 → 0019` 已在上一轮真实通过，因此测试现对空库执行首次升级、对 `0018/0019` 合法起点直接进入阶段 12 边界回退，不修改已发布迁移，也不把无关的全历史回退加入重复验收。普通后端回归仍为 `489 passed, 29 deselected`；等待重跑 4.1。
- 阶段 14 PostgreSQL 4.1 已于 2026-07-29 真实通过 8、失败 0，测试断言最终 revision 为 `20260728_0019`。随后错误来自误执行仅供生产授权后使用的 `MIGRATION_DATABASE_URL` 命令，以及本项目不支持的 Alembic `-x database_url` 参数，不影响 4.1 结果。验收文档已改为 A/B/C 终端体系，生产命令明确要求当前跳过。
- 4.1 通过后的额外 Alembic `current` 查询遇到 `socket.gaierror`，属于 Supabase Direct 主机名的瞬时 DNS 解析失败，不是迁移失败。该查询与测试内部的最终 revision 断言重复，已从 4.1 删除；不重跑、不降级数据库，下一步保持 4.2。
- 阶段 14 第 4.2 节已于 2026-07-30 真实通过 3、失败 0：Auth/JWT、教师停用与恢复、管理员边界通过 2，修正验收测试签名路径后 Storage 补测通过 1；下一步进入第 5 节 Redis/Celery 与 Worker 验收。
- 阶段 14 第 5 节已于 2026-07-30 完成真实验收：专属临时 Redis 心跳通过 1、失败 0；三个 Worker 精确队列隔离、维护任务、双向执行槽隔离、丢失收口和 600 秒重投均通过。恢复导出领取 2 次、完成 1 次，OOXML 有效，Redis、Storage、数据库夹具和临时密码均已清理；用户随后确认第 6.1 节及之前全部完成。
- 阶段 14 第 6.1 节已由用户确认完成，旧的 VPN fake-IP、HTTP 422 和供应商未通过状态不再作为当前阻塞。
- 阶段 14 第 6.2 节最终决定：不执行新的 100 次真实模型调用。真实输入解析 100 通过、失败 0、重复哈希 0；结合阶段 10 的 100 篇批次完整性、当前 100 篇唯一投递自动化、阶段 12 的 100 篇 Excel 映射、当前头权限/配额、真实 Worker 和供应商冒烟，组合证据已足够。以后如需验证耗时、并发、失败率或费用，先定义 SLA 后作为独立性能测试。
- 阶段 14 发布候选自检修复真实 E2E 重复计费、API 缺少安全响应头、部署后队列/签名 URL/告警/回滚恢复证据不足和密钥扫描宽泛基线五类缺口。真实 E2E 现在只创建一个批次，手机端复用同一结果；密钥基线为空，只对逐行人工确认的测试假值和界面文案显式放行，负向控制仍会失败。
- 第 5 节最终自检：普通后端 489 项、目标回归 23 项和密钥扫描修复回归 1 项通过，失败 0；严格 mypy 170 个源文件、Ruff、格式、仓库密钥扫描和差异检查通过。扫描修复只为两条固定假数据库 URL 增加显式测试白名单注释，没有修改基线或忽略真实候选。
- 2026-08-30 阶段 14 正式验收已压缩为 6 个节点，并补齐终端 A/B/C/D、Supabase、Sites、
  UptimeRobot 和 Chrome 的逐步操作。最终独立复查补齐所有 Worker 离线、Sites 原版本恢复、
  封存目录构建、失败停机、双端恢复验证、无尾斜杠 HTTPS origin 和 1 分钟 Heartbeat/2 分钟
  grace 要求。环境脚本会隐藏 Heartbeat URL并拒绝错误 origin；本地门禁、32 项聚焦测试、
  29 个 zsh 块和 10 个 Python heredoc 均通过，失败 0。旧候选不含这些修复，必须生成新候选并
  取得 CI 8/8；生产和付费操作未执行。
- 新候选 `27c67ac` 的 GitHub CI 前 7 项通过，第 8 项源码密钥扫描因两个已知完整 Git SHA 在
  测试和四个验收命令变量中的 6 处文本被高熵规则误判而失败；没有发现真实凭据。6 行均已添加
  精确 allowlist 注释，不修改空基线或扫描强度；须从该修复生成新候选并重新取得 CI 8/8。
- 节点 2.1 首次运行发现旧脚本先创建 0 字节 `tailscaled.state`，当前 Tailscale daemon 因此稳定
  返回 `state store is unhealthy`。差分测试确认“文件不存在”可进入 `NeedsLogin`，“空文件”在
  `up` 时失败。三个启动入口已改为不预建状态文件，并新增空文件失败关闭回归；正式空文件只允许
  移动到同目录隔离备份，不自动删除。候选 `d99dd5f` 的 CI 已只读确认 8/8，但不含该运行时修复，
  因此降为历史候选；须从当前修复生成新候选并重新取得 8/8。
