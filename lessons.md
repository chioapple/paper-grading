# Lessons

- 用户明确选择个人、非商业、零基础设施月费落地后，不再把 Render 付款或资源创建列为
  阶段 14 阻塞；前端改用 Sites，后端、Redis 和 Worker 改由常开 Mac 承载，部署环境
  风险通过 Funnel、`launchd`、防休眠、外部告警和双版本回滚验收。
- 阶段 11 终端 D 的辅助脚本若调用 `Settings.load()`，必须在同一命令块显式设置 `REDIS_URL`；终端 A 的环境变量不会自动传到终端 D。
- 评分 item 的 `needs_review` 同时承载成功结果待确认和模型结果无效待人工处理；真实验收必须额外断言 `review_available=true` 且存在 succeeded attempt，不能只看待复核数量。
- 当可信 Rubric 使用中文而反馈要求英文时，提示词必须明确要求翻译或释义 Rubric 名称与描述，不能只写“使用英文”；提示词语义变化必须升版本，并保留历史版本的精确哈希重建。
- 项目内的开发任务应自动检索并使用当前实际可用的相关 skills，不等待用户逐次点名。
- 日常回复不重复列出使用的 skills；仅在用户询问、能力不可用、规则冲突、任务暂停或上级规则要求时说明。
- 插件缓存不等于当前可调用能力，使用前必须核对当前会话是否实际暴露。
- 用户要求在本地终端自行执行破坏性复验时，只返回可复制命令，不代为执行或追加诊断；收到结果后再继续收尾。
- Supabase Auth 邀请必须按真实的“先插入用户、再更新 `invited_at`”顺序设计和验收触发器；邮件回调服务必须在点击一次性链接前启动。
- 用户已明确判定 secret key 问题解决；后续阶段 3 状态汇报不再把该项列为阻塞或待办。
- 真实模型 API Key 一旦出现在聊天中即视为泄露；不得使用、保存或写入终端，必须要求用户撤销并只在本地或产品管理页输入替代 Key。
- Alembic 使用项目命名约定时，迁移中引用数据库已有的完整约束名必须通过 `op.f(...)` 标记为已格式化；离线迁移测试必须断言最终 SQL 名称，防止表名前缀和 `_check` 后缀被重复添加。
- 本地应用启动也不得把 `TEST_MIGRATION_DATABASE_URL` 当作 `DATABASE_URL`；迁移使用 direct 地址，FastAPI 持久连接池使用 Supavisor session pooler 5432，配置流程必须分别索取并验证两种入口。
- 阶段验收中的 `.env.stage2-test` 只用于独立测试库的迁移回放；本地 API 和 Worker 必须加载应用配置文件（当前为 `.env.stage7-local`），且需要周期补发时 Worker 命令必须包含 `--beat`。
- 用户使用会切换节点的 VPN/代理时，所有 Supabase 设置和连接调整只提供文字流程；一次真实验收期间固定节点，并区分浏览器代理与 PostgreSQL TCP 的实际出口。
- Supabase allow list 报错前先确认用户当时是否切换了梯子；若网站既定为关闭梯子运行，不要要求把临时代理出口 IP 加入允许项，应先关闭梯子再复验。
- 给用户返回 macOS 终端诊断命令时不能假定安装了 `rg`；用户侧环境检查优先使用系统自带命令，项目内代码搜索仍使用 `rg`。
- 诊断页面请求失败时，不能用事后“端口当前未监听”推断请求发生时后端未启动；应以请求时的后端访问日志和异常栈为准。
- 用户明确确认阶段验收文件全部步骤正确且符合预期后，应直接按最终验收信号收口该阶段；不要继续把已确认项目当成待排障项，也不要要求为可选回放重新降级数据库。
- 验收命令中的前置 API 写入必须先记录并断言 HTTP 状态，再解析 JSON；不能把响应直接重定向后继续执行，否则空的 500 响应会被后续 JSON 错误掩盖。受限角色下的 `SELECT ... FOR UPDATE` 还必须与表级更新权限一起做真实 PostgreSQL 回归。
- 用户明确说明文件仅供参考且不保证正确时，该文件不是交付物或验收真源；只从中提取线索，最终以项目正式验收文档和真实测试证据为准。
- SQLAlchemy 的 `ChunkedIteratorResult` 带有 `keys()`，不能直接交给 `dict()`；统计查询结果应显式逐行解包，并用保留真实结果协议的仓储公开行为测试覆盖。验收命令中的环境文件名必须从正式文档复制，不能把 `.env.stage2-test` 或 `.env.stage7-local` 拼成不存在的 `.env.stage7-test`。
- 真实迁移回放不要把每个预检、目录核验和 Alembic 操作都放进新的 `NullPool` direct 连接；应由验收流程建立一次连接并注入 Alembic，在同一连接内串行完成回放。这样减少握手面，不用重试掩盖真实不可达；一旦开始降级，异常恢复必须覆盖整个可变更区间并最终回到迁移头。
- PostgreSQL 延迟约束测试若用外层事务整体回滚，必须在回滚前显式 `SET CONSTRAINTS ... IMMEDIATE`；否则只证明写入语句和保存点成功，不能证明真实 COMMIT 成功。共享触发器处理不同表的 `record NEW` 时，不得在同一表达式引用各表专属字段，应使用分开的控制流语句。
- 多终端验收中，每段命令必须明确标出执行终端；依赖终端内函数的控制命令不能只写“然后执行”，否则容易在仅负责 SSE 的终端运行并产生次生文件错误。
- 可复制验收命令不能把中文说明或 `<PLACEHOLDER>` 放进可直接执行的变量赋值；需要用户提供 UUID 时必须使用交互式 `read` 并在发请求前校验格式。涉及并发观察时，先建立观察连接，再启动执行进程，避免人工输入期间错过瞬时状态。
- 多终端验收不能只在章节开头说明终端职责；每一个可复制命令块前都必须再次标明执行终端，并明确它依赖的终端内函数和变量。跨章节引用启动命令时应直接展开完整命令，避免用户在错误终端凭记忆执行。
- 会创建真实计费批次的并发验收必须在 POST 前明确确认 Worker 正在运行；观察超时后先从已保存响应恢复批次 ID 和状态，禁止因“没看到 running”直接创建重复批次。
- Celery 防饥饿不能只测试任务路由到不同队列；若同一个单并发 Worker 同时消费评分与维护队列，慢维护任务仍可占满唯一执行槽。必须用独立消费者或独立并发槽做真实运行回归，并验证评分队列在维护任务阻塞时仍能前进。
- 用户在了解风险后明确接受某项安全例外时，应记录例外及后续复核阶段，不再反复要求撤销；同时不得把例外描述为安全配置或最佳实践。
- 后续阶段的真实 PostgreSQL 回归不能无条件复用早期阶段“业务表必须全空”的保护前提。若测试使用随机主键、外层事务和最终整体回滚，应只要求本阶段迁移真正需要为空的表，并在回滚后断言既有数据计数完全不变。
- 共享外层事务的真实 PostgreSQL 测试中，仓储执行的 `SET LOCAL ROLE` 不能被假定在保存点结束后自动恢复。需要管理员权限的夹具必须在首次受限仓储调用前准备；跨教师隔离应断言目标新资源不可见，不能要求另一教师的历史列表为空。
- 访问令牌验收不能把中文说明写成可执行赋值；应通过本机交互登录取得真实 token，先调用 `/auth/me` 核验教师身份，再访问业务接口。后端还必须在外部请求前拒绝非 ASCII、控制字符和空白 token，使误填稳定返回 401，不能泄成 500。
- 阶段验收的普通 pytest、marker 注册和默认 PostgreSQL 排除依赖 `backend/pyproject.toml`，必须从 `backend/` 目录运行；从仓库根目录直接执行会把真实 PostgreSQL 测试误当普通测试。
- 多终端验收必须是一条资源安全的有序流程：迁移时停 API/前端/Worker，真实计费前保持 Worker 停止，重评后重新生成当前 attempt 草稿，HTTP 先断言状态再解析，结束时只清理本次 `mktemp` 目录。
- 验收环境变量预检必须直接来自实际 Settings 模型和 `.env.example`；不得要求代码未读取的变量，也不能漏掉 Settings 中必需的破坏性操作确认值。
- 多步骤共用的交互终端中，验收命令的 `set -e` 必须放在子 Shell 内；否则一次预期的预检失败会污染后续步骤，甚至直接关闭该终端。
- zsh 中不要把 `path` 用作普通循环变量；它会同步覆盖 `PATH`。验收命令统一使用
  `script_path` 等名称，并避免在共享终端启用会影响 session-save hook 的顶层 `set -u`。
- 只面向 macOS 的运维脚本测试必须显式分层：Ubuntu CI 验证静态契约，目标 Mac 执行
  Tailscale、Homebrew 和 `launchctl` 行为。不能让跨平台 CI 假装拥有目标机系统工具。
- 确定性文件测试不能连续调用两次后碰巧同秒通过；凡第三方库会在保存时注入当前时间，
  必须在测试中强制推进时钟，并在最终归档字节层恢复冻结快照时间。
- 推送前不能只格式化“本轮主要文件”；必须执行 CI 中完全相同的目录级 Ruff 命令，否则
  同一提交链里的较早改动可能在本机局部检查中遗漏。
- 真实 PostgreSQL 验收必须按用途分离连接：Alembic 回放使用 Direct URL，应用权限、RLS 和事务契约使用同项目 Supavisor Session Pooler 5432。TCP 端口成功不能证明 PostgreSQL TLS 握手稳定，也不能让普通权限测试反复依赖 IPv6 Direct 新连接。
- `needs_review` 还可能表示模型调用结果未知或失败，不能据此断言存在可复核分数。列表和验收脚本必须检查当前 `dispatch_version` 是否有成功 attempt；无成功结果时只允许经过费用确认的原模型重评。
- 任何对话只要要求重新打开本项目前端，都必须同时展开完整的启动顺序和命令：先检查 Redis 与端口，再启动 FastAPI，确认 `/health/live` 和 `/health/ready`，最后启动 Vite；不得只给前端网址或假定其他终端仍在运行。长期进程必须分别标明终端，已有健康进程时禁止重复启动。
- 前端启动命令必须先加载 `.env.stage7-local`，再显式传递 `VITE_API_BASE_URL`、`VITE_SUPABASE_URL` 和 `VITE_SUPABASE_PUBLISHABLE_KEY`；只启动 Vite 或只设置 API 地址会让入口脚本因缺少 Supabase 浏览器配置而在渲染前崩溃，表现为纯空白页。
- 用户明确允许本地 VPN fake-IP 时，只能用默认关闭的显式开发开关放行内置官方域名的 `198.18.0.0/15`；生产、自定义地址和其他私网继续拒绝，Worker 停止后立即关闭并做反向回归。
- “英文反馈”不能只依赖提示词或前端标签。模型和教师草稿的评分理由、扣分理由、修改建议与总体反馈都必须由后端执行确定性英文脚本校验；契约变化必须升级提示词版本，旧 attempt 保持不可变。
- Worker 的普通 INFO 日志不得输出私有 Storage 对象路径；第三方 HTTP 客户端请求日志必须在 Worker 中降到 warning，失败只记录稳定错误码。
- Alembic 离线 SQL 能生成不代表 PostgreSQL 能执行；`COALESCE`、`GREATEST`、`LEAST` 是特殊表达式，不能加 `pg_catalog.` 前缀。迁移测试必须显式阻止这类限定写法，并以真实 PostgreSQL 回放作为最终证据。
- 独立 Redis 验收应启动本轮专属、仅绑定本机且关闭持久化的临时实例，不能用共享实例的不同逻辑库冒充独立资源；结束时先停 Worker、确认队列和未确认消息归零，再关闭临时 Redis。
- 自定义 Supabase Worker 角色轮换密码后，必须先用该角色执行只读连接探针再启动 Celery。Worker 显示 ready 只证明 Redis 可用，不能证明数据库密码已由 Session Pooler 接受。
- 跨进程外部验收需要持久化不可变快照时，夹具清理不能逐表 DELETE。只有相关表总数与本轮夹具精确相等且外键闭包完整时，才允许在独立测试项目按既有测试契约整体清空；否则立即停止。
- 用户要求“简易验收流程”时，只精简节点数量和重复背景，不能删除执行方法。正式交付仍须
  逐块标明终端或网页位置、可复制命令、输入来源、通过标准和失败停止条件。
- 验收中的回滚不能吞掉恢复错误；失败处理必须先取消自身 trap，切回原完整 SHA，启动并轮询验证
  实际 release。Sites 的部署、状态、访问范围、页面和 API 任一失败，都属于同一恢复边界。
- 完整 Git SHA 出现在 Python 字符串或 shell 变量中可能被高熵规则误判；只能在人工确认其确为
  Git 对象后对具体行添加 allowlist 注释，并重跑 CI 原始扫描。不得放宽插件、写入基线或隐藏真实候选。
- `tailscaled --state=<path>` 首次启动必须让状态路径不存在；预先 `touch` 出 0 字节文件会使
  `tailscale up` 返回 state store unhealthy。启动脚本应拒绝空状态并保留恢复副本，不能把空文件
  当成安全初始化，也不能自动覆盖非空身份状态。
- 验收中的 Git 提交和远端推送必须拆成两个命令块。提交成功但推送遇到网络错误时，只允许重试
  可幂等的推送与远端 SHA 核验块，不能让第二次 `git commit` 的“无文件可提交”阻断恢复流程。
- 用户要求阶段验收完全免费时，必须先定义可核验口径并把它变成代码硬门禁，不能只靠操作提醒。
  阶段 14 以“新增费用为 0”为可保证边界：供应商调用必须从 API、Worker、环境生成和运行验证
  多层关闭；账户方案或 Sites 发布价格无法确认时直接停止。既有设备、网络、用电或已购买套餐
  不是免费服务，不能被文档隐瞒为总成本 0。
- 外部 CLI 的帮助文本不能替代真实版本行为。Tailscale 1.98 的 `serve get-config` 必须把
  `--all` 放在重定向前并从 stdout 原子保存；把输出文件当位置参数会让后续 `--all` 不被解析。
- 会被 shell `source` 的生产环境文件不能原样拼接用户输入；每个值必须拒绝换行并由 shell
  安全转义，再用实际 Settings 加载结果校验，防止 `$()`、美元符号、引号或空格改变配置或执行命令。
- Sites Worker 测试不能由测试夹具直接替 `ASSETS` 返回 `index.html` 后就宣称部署包可用；必须增加
  `ASSETS` 存在但始终返回 404 的回归，并证明 Worker 自身仍能返回 SPA 入口和真实构建 JS。正式
  owner 请求返回 404 时，候选立即作废，修复后必须生成新 SHA、取得 CI 8/8 并重做 2.3—2.5。
- 把构建文件内嵌进 JavaScript 时，不能把任意内容直接作为 `String.replace` 的 replacement 字符串；
  其中的 `$&`、美元符号加反引号等会被解释为替换模式。必须使用 replacement 函数，并让生成 Worker 的导入测试
  覆盖这一点。Base64 内嵌还会触发密钥扫描，应使用文本或明确字节数组并保留原扫描强度。
- 部署适配发生变化时，候选修好不等于回滚仍可用。节点 2.4 保存版本前必须让两个封存 Worker 都在
  外部 `ASSETS` 始终返回 404 的同一门禁下返回页面 200；旧回滚失败时应升级回滚基线并重新取得
  候选 CI，不能先部署候选再把已知 404 的版本保留为回滚。
